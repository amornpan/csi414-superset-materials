# แผนสาย EDA สัปดาห์ 5–10 + สัปดาห์ 11 บทสรุป — สำรวจข้อมูล `flights` ทีละขั้นจนส่งต่อให้คนสร้างโมเดลได้ แล้วปิดด้วย Superset ในงานจริงและ AI ผ่าน MCP

> ร่างเพื่อพิจารณา 19 ก.ย. 2569 · ตัวเลขทุกตัวในเอกสารนี้ตรวจกับข้อมูลจริงบนเซิร์ฟเวอร์ของวิชาแล้ว
> (Superset 4.1.1 · dataset `flights` ในฐาน `examples` ซึ่งเป็น SQLite 3.40 · นักศึกษาทุกคนมีสิทธิ์เข้าถึงแล้ว)

## หลักการ

- **ชุดข้อมูลเดียวตลอด 6 สัปดาห์** เพราะ EDA เป็นกระบวนการ ไม่ใช่เทคนิคเป็นชิ้น ๆ สิ่งที่พบสัปดาห์นี้กำหนดสิ่งที่ทำสัปดาห์หน้า
- **สายเดิม (`tutorial_flights` + ตัวตรวจอัตโนมัติ) ไม่เปลี่ยน** สาย EDA ใส่เป็นตอนท้ายของแล็บ ~35 นาที บนแท็บ `สำรวจข้อมูล flights` ของแดชบอร์ดใบเดิม
- **ทุกสัปดาห์จบด้วยการตัดสินใจ ไม่ใช่แค่การเห็น** จดลง Decision log (สิ่งที่พบ · หลักฐาน · ตัดสินใจ · เหตุผล) สะสมจนเป็นใบสรุปสัปดาห์ 10
- **คาบทฤษฎีสอนแนวคิด คาบแล็บลงมือ ควิซถามการอ่านกราฟ** ของขั้นเดียวกันในสัปดาห์เดียวกัน
- **`video_game_sales` ใช้สาธิตในคาบทฤษฎีเท่านั้น** อาจารย์ทำให้ดูบนชุดหนึ่ง นักศึกษาทำเองบนอีกชุดหนึ่ง

## ชุดข้อมูล `flights` — สิ่งที่ตรวจแล้ว

| เรื่อง | ค่าจริง |
|---|---|
| ขนาด | 55,105 แถว · 44 คอลัมน์ · รวมพิกัดสนามบินต้นทาง/ปลายทางมาให้แล้ว |
| ช่วงเวลา | **4 วัน** 1–4 ม.ค. 2015 (พฤ–อา) — ไม่ใช่ทั้งปี ต้องบอกนักศึกษาตั้งแต่สัปดาห์ 5 |
| สายการบิน / สนามบิน | 14 สาย · ต้นทาง 312 แห่ง (WN มากสุด 11,472 เที่ยว · VX น้อยสุด 573) |
| ค่าหาย | `DEPARTURE_DELAY` 1,195 (= ยกเลิก) · `ARRIVAL_DELAY` 1,354 (= ยกเลิก 1,232 + เปลี่ยนเส้นทาง 122) · สาเหตุความล่าช้า 37,299 (บันทึกเฉพาะเมื่อ `ARRIVAL_DELAY >= 15` ตรงกันเป๊ะ 17,806) · `LATITUDE` 35 (สนามบิน ECP, PBG, UST) |
| การกระจายของ delay | median 2 นาที แต่ mean 16.8 · p95 = 86 · p99 = 183 · max **1,380** (23 ชั่วโมง) · ออกก่อนเวลา 22,282 เที่ยว |
| Outlier | delay > 180 นาที = 557 เที่ยว · 5 อันดับแรกเป็น AA/DL 1,112–1,380 นาที ค่า delay ขาออกกับขาเข้าสอดคล้องกัน = ของจริง ไม่ใช่ข้อมูลผิด |
| ความสัมพันธ์ (Pearson r) | DISTANCE~AIR_TIME **0.976** · DEPARTURE_DELAY~ARRIVAL_DELAY **0.95** · DISTANCE~ARRIVAL_DELAY −0.015 · TAXI_OUT~DEPARTURE_DELAY 0.044 |
| รูปแบบรายชั่วโมง | สัดส่วนเที่ยวบินออกช้า ≥ 15 นาที: 06:00 = 11.6% → 13:00 = 35.8% → 21:00 = **49.0%** (ข้อค้นพบหลักของทั้งชุด) |
| ซ้ำ | ไม่มีแถวซ้ำ (ตรวจด้วยคีย์ วันที่+สายการบิน+เที่ยวบิน+ต้นทาง) |
| เป้าหมายโมเดล | เที่ยวบินถึงช้า ≥ 15 นาที = **33.1%** · อัตราตรงเวลารายสาย VX 86.2% … MQ 46.7% |
| ข้อจำกัด SQL | SQLite: ไม่มี `percentile_cont`/`corr()` แต่มี `sqrt` และ window function (`NTILE`) พอคำนวณเองได้ · มี `rowid` ใช้เป็นรหัสแถว |

---

## สัปดาห์ 5 — อะไรหายไป และหายเพราะอะไร

*(ร่างใบงานฉบับเต็ม: `lab05-แดชบอร์ด/ใบงาน_ร่างEDA.md`)*

| | |
|---|---|
| **คำถาม EDA** | ข้อมูลครบไหม หายตรงไหน หายแบบไหน |
| **กราฟ** | Table ไม่มี dimension ใส่ `COUNT(*)` กับ `COUNT(คอลัมน์)` · Table เดิม + dimension `CANCELLED`, `DIVERTED` · ตัวกรอง `ARRIVAL_DELAY >= 15` |
| **นักศึกษาค้นพบเอง** | delay ขาเข้าหาย 1,354 = ยกเลิก 1,232 + เปลี่ยนเส้นทาง 122 ลงตัวพอดี · สาเหตุความล่าช้าถูกบันทึกเฉพาะเมื่อช้า ≥ 15 นาที |
| **แนวคิดในคาบทฤษฎี** | ค่าหาย 3 แบบ: หายเพราะเหตุการณ์ · หายเพราะกฎ · หายโดยไม่มีคำอธิบาย และทำไม "ลบแถวที่มีค่าหาย" จึงอันตราย |
| **สาธิตด้วย `video_game_sales`** | `Year` หาย 271 · `Publisher` หาย 58 — หายโดยไม่มีคำอธิบาย ต่างจาก `flights` ที่อธิบายได้ทุกแถว |
| **ตัดสินใจ** | แยกเที่ยวบินยกเลิกออกจากการวิเคราะห์ delay แต่เก็บไว้ตอบคำถามเรื่องการยกเลิก · ห้ามเติมสาเหตุความล่าช้าด้วย 0 |
| **ควิซ** | ให้ตาราง COUNT แล้วถามว่าคอลัมน์ไหนหายกี่แถว และหายแบบไหน |

## สัปดาห์ 6 — ตัวแปรกระจายตัวอย่างไร

| | |
|---|---|
| **คำถาม EDA** | ค่าปกติอยู่ตรงไหน เบ้ไหม กลุ่มไหนต่างจากกลุ่มอื่น |
| **กราฟ** | **Histogram** ของ `DEPARTURE_DELAY` (ลองจำนวน bin 10 → 50 → 100) · Bar จำนวนเที่ยวบินตาม `AIRLINE` · Bar ตาม `DAY_OF_WEEK` · เปิด **cross-filter** (ข้อกำหนดสัปดาห์ 6 อยู่แล้ว) คลิกสายการบินแล้วดู histogram เปลี่ยน |
| **นักศึกษาค้นพบเอง** | histogram ช่วง −39 ถึง 1,380 ดูไม่รู้เรื่อง ต้องกรอง `<= 180` ถึงเห็นรูป แต่การกรองทิ้ง 557 เที่ยวต้องเขียนกำกับ · mean 16.8 กับ median 2 เล่าคนละเรื่อง · `DAY_OF_WEEK` มีแค่ 4 ค่า = ข้อมูลมีแค่ 4 วัน · AS median −2 แต่ MQ median 9 |
| **แนวคิดในคาบทฤษฎี** | histogram กับการเลือก bin · ความเบ้และหางยาว · mean vs median · ทำไมต้องดูการกระจายก่อนสรุปค่าเฉลี่ย |
| **สาธิตด้วย `video_game_sales`** | `Global_Sales` median 0.17 ล้าน แต่ max 82.74 (Wii Sports) — หางยาวสุดขั้ว ถ้าใช้ mean จะได้ภาพเกมทั่วไปผิดไปหลายเท่า |
| **ตัดสินใจ** | รายงาน delay ด้วย median และเปอร์เซ็นไทล์ ไม่ใช่ mean · กราฟที่โชว์คนอื่นตัดแกนที่ 180 พร้อมระบุจำนวนที่ตัด · จดว่าข้อสรุปทั้งหมดใช้ได้กับ 4 วันแรกของปีเท่านั้น |
| **ควิซ** | ให้ histogram สองรูป bin ต่างกัน ถามว่ารูปไหนซ่อนอะไร · ให้ mean กับ median ถามทิศทางความเบ้ |

## สัปดาห์ 7 — ค่าไหนสุดโต่ง และเป็นของจริงหรือข้อมูลผิด

| | |
|---|---|
| **คำถาม EDA** | outlier อยู่ไหน มีกี่ตัว เชื่อได้ไหม จะทำอะไรกับมัน |
| **กราฟ** | **Box Plot** `DEPARTURE_DELAY` แยกตาม `AIRLINE` เลือก whisker = **Tukey (1.5×IQR)** แล้วสลับเป็น min/max และเปอร์เซ็นไทล์ 2/98 เพื่อดูว่า "outlier" เปลี่ยนตามนิยาม · Table เรียง `DEPARTURE_DELAY` มาก→น้อย 20 แถว โชว์ `ARRIVAL_DELAY` และ `ds` ข้าง ๆ · Box Plot ตาม `DAY_OF_WEEK` |
| **นักศึกษาค้นพบเอง** | AA: Q3 = 30, ขอบบน 1.5×IQR = 79.5, นอกขอบ 423 เที่ยว แต่ VX ขอบบนแค่ 18.5 — "outlier" ขึ้นกับกลุ่ม · 5 อันดับแรก 1,112–1,380 นาที มี delay ขาเข้าใกล้เคียงกันทุกแถว จึงเป็นของจริง ไม่ใช่พิมพ์ผิด · ค่าลบสุด −39 คือออกก่อนเวลา ไม่ใช่ error |
| **แนวคิดในคาบทฤษฎี** | อ่าน box plot (median, Q1, Q3, whisker, จุด) · กฎ 1.5×IQR · outlier ที่เป็นของจริง vs ข้อมูลผิด และวิธีแยก (ดูคอลัมน์ข้างเคียงว่าสอดคล้องไหม) · ทางเลือก: ลบ / cap / เก็บแต่ flag |
| **สาธิตด้วย `video_game_sales`** | Box Plot `Global_Sales` ตาม `Genre` — Wii Sports โดดออกจากทุกกล่อง เป็น outlier จริงที่ **ห้ามลบ** เพราะมันคือคำตอบของคำถาม "เกมอะไรขายดีที่สุด" |
| **ตัดสินใจ** | เก็บ outlier ไว้ทั้งหมด (ของจริง) แต่ **flag** แถวที่ `DEPARTURE_DELAY > 180` (557 แถว ≈ p99) สำหรับกราฟและโมเดล · สรุปรายสายด้วย median |
| **ควิซ** | ให้ Q1, Q3 คำนวณขอบ 1.5×IQR แล้วชี้ว่าค่าไหนเป็น outlier · ให้ box plot สองสายถามว่าสายไหนแปรปรวนกว่า |

## สัปดาห์ 8 — ยืนยันสิ่งที่ตาเห็นด้วยตัวเลข และสร้างชุดข้อมูลที่พร้อมใช้

| | |
|---|---|
| **คำถาม EDA** | ที่เห็นมา 3 สัปดาห์จริงแค่ไหน · นิยาม "ล่าช้า" ให้ทั้งทีมใช้เหมือนกันคืออะไร |
| **SQL Lab** (SQLite) | (1) นับ null ทุกคอลัมน์ในคำสั่งเดียว `SUM(CASE WHEN x IS NULL THEN 1 ELSE 0 END)` (2) ตรวจแถวซ้ำ `COUNT(*)` vs `COUNT(DISTINCT ds||AIRLINE||FLIGHT_NUMBER||ORIGIN_AIRPORT)` → 0 (3) เปอร์เซ็นไทล์ด้วย `NTILE(100) OVER (ORDER BY DEPARTURE_DELAY)` (4) Pearson r จากสูตร `(nΣxy − ΣxΣy) / sqrt((nΣx² − (Σx)²)(nΣy² − (Σy)²))` — ได้ 0.95 ตรงกับที่ scatter สัปดาห์ 9 จะโชว์ |
| **Virtual dataset `flights_clean`** (ข้อกำหนดสัปดาห์ 8) | `SELECT rowid AS id, *, SCHEDULED_DEPARTURE/100 AS dep_hour, CASE WHEN ARRIVAL_DELAY >= 15 THEN 1 ELSE 0 END AS is_delayed, CASE WHEN DEPARTURE_DELAY > 180 THEN 1 ELSE 0 END AS is_outlier FROM flights WHERE CANCELLED = 0 AND DIVERTED = 0` → 53,751 แถว = ผลของการตัดสินใจสัปดาห์ 5 และ 7 กลายเป็นของจริง |
| **Metric กลาง** (ข้อกำหนดสัปดาห์ 8) | `on_time_rate = AVG(1 − is_delayed)` นิยามไว้ที่ dataset ให้ทุกกราฟใช้ตัวเดียวกัน — VX 86.2% … MQ 46.7% |
| **แนวคิดในคาบทฤษฎี** | ทำไมนิยาม metric ต้องอยู่ที่เดียว (semantic layer) · reproducibility: การตัดสินใจใน EDA ต้องกลายเป็นโค้ดที่รันซ้ำได้ ไม่ใช่กดในหัว |
| **ตัดสินใจ** | ทุกกราฟตั้งแต่สัปดาห์ 9 ใช้ `flights_clean` เท่านั้น · "ล่าช้า" = ถึงช้า ≥ 15 นาที ตามนิยามของ BTS ที่ข้อมูลใช้อยู่แล้ว (สาเหตุความล่าช้าถูกบันทึกที่เกณฑ์นี้พอดี) |
| **ควิซ** | ให้ SQL นับ null ถามผลลัพธ์ · ถามว่าทำไมต้องกรอง `CANCELLED = 0` ก่อนคำนวณ delay เฉลี่ย |
| **หมายเหตุ** | ตรงกับ Progress ของโปรเจกต์ร่วม (12–17 ต.ค.) ทีมมีบันไดคำถาม + Decision log ไปใช้กับข้อมูลตัวเองได้พอดี |

## สัปดาห์ 9 — อะไรสัมพันธ์กับอะไร และ outlier บิดภาพแค่ไหน

| | |
|---|---|
| **คำถาม EDA** | ตัวแปรไหนไปด้วยกัน ตัวไหนซ้ำซ้อน ตัวไหนใช้ทำนายไม่ได้เพราะรู้ทีหลัง |
| **กราฟ** | **Bubble Chart** จาก `flights_clean` entity = `id` (จุดต่อแถวจริง กรองสายการบินเดียวก่อนเพื่อไม่ให้หนัก) 3 คู่: `DISTANCE`~`AIR_TIME` · `DEPARTURE_DELAY`~`ARRIVAL_DELAY` ลงสีด้วย `is_outlier` · `DISTANCE`~`ARRIVAL_DELAY` · Scatter แบบรวมค่า: `dep_hour` กับ `on_time_rate` · **แผนที่ deck.gl** (ข้อกำหนดสัปดาห์ 9): จุดสนามบินต้นทาง ขนาด = จำนวนเที่ยว สี = delay เฉลี่ย |
| **นักศึกษาค้นพบเอง** | ระยะทางกับเวลาบิน r = 0.976 เกือบเป็นเส้นตรง = ตัวแปรซ้ำซ้อน ใส่ทั้งคู่ในโมเดลไม่ได้อะไรเพิ่ม · delay ขาออกกับขาเข้า r = 0.95 แต่ **รู้ delay ขาออกได้ก็ต่อเมื่อเครื่องออกไปแล้ว** = leakage ถ้าจะทำนายล่วงหน้า · ระยะทางกับ delay r = −0.015 ไม่เกี่ยวกันเลย · ชั่วโมงกับสัดส่วนล่าช้า 11.6% → 49% ชัดที่สุดในชุด · ATL 3,202 เที่ยว delay เฉลี่ย 13.2 แต่ DFW 2,766 เที่ยว 33.0 · สนามบิน 3 แห่งไม่ขึ้นบนแผนที่ (จดไว้ตั้งแต่สัปดาห์ 5) |
| **แนวคิดในคาบทฤษฎี** | อ่าน scatter: ทิศทาง ความแรง รูปทรง · correlation ≠ causation · leakage · แผนที่คือ scatter ที่แกนเป็นพิกัด |
| **สาธิตด้วย `video_game_sales`** | `NA_Sales`~`EU_Sales` เห็นความสัมพันธ์บวกแต่มี Wii Sports ดึงมุมกราฟ ลองกรองออกแล้วดูว่ารูปเปลี่ยนไหม |
| **ตัดสินใจ** | เลือกตัวแปรที่รู้ก่อนออกเดินทางเท่านั้นสำหรับโมเดลทำนาย (`dep_hour`, `AIRLINE`, `ORIGIN_AIRPORT`, `DISTANCE`, `DAY_OF_WEEK`) · ตัด `AIR_TIME` (ซ้ำกับ `DISTANCE`) และ `DEPARTURE_DELAY`/`TAXI_OUT`/`WHEELS_OFF` (รู้ทีหลัง) |
| **ควิซ** | ให้ scatter 3 รูป จับคู่กับค่า r · ถามว่าตัวแปรไหนใช้ทำนายล่วงหน้าไม่ได้และเพราะอะไร |

## สัปดาห์ 10 — สรุปให้คนสร้างโมเดลใช้ต่อ

| | |
|---|---|
| **คำถาม EDA** | คนที่จะสร้างโมเดลควรรู้อะไรก่อนแตะข้อมูล |
| **ชิ้นงาน: Data Understanding Brief** (Markdown บนแท็บสำรวจข้อมูล + กราฟที่คัดแล้ว 4–5 รูป) | 1. **เป้าหมาย**: `is_delayed` 33.1% — ไม่สมดุลระดับปานกลาง 2. **ตัวแปรที่น่าใช้**: ชั่วโมง สายการบิน สนามบิน วัน 3. **ห้ามใช้ (leakage)**: ทุกคอลัมน์ที่รู้หลังออกเดินทาง 4. **ค่าหายและกฎ**: 3 แบบจากสัปดาห์ 5 5. **outlier**: 557 แถว flag ไว้ ไม่ลบ 6. **ข้อจำกัด**: 4 วันแรกของปี ช่วงวันหยุด ไม่แทนทั้งปี 7. **Decision log ฉบับเต็ม** |
| **ข้อกำหนดสัปดาห์ 10 เดิม** | export ครบชุด · ตั้งชื่อกราฟทุกรูป · cache timeout — brief นี้คือสิ่งที่ถูก export ไปพร้อมกัน |
| **แนวคิดในคาบทฤษฎี** | exploratory → explanatory: จากกราฟ 20 รูปที่ทำให้ตัวเองเข้าใจ เหลือ 5 รูปที่ทำให้คนอื่นเข้าใจ · เขียนข้อจำกัดของข้อมูลให้คนที่ไม่ได้ทำ EDA อ่านแล้วไม่ใช้ผิด |
| **ตัดสินใจ** | จัดลำดับว่าปัญหาข้อมูลข้อไหนต้องแก้ก่อนสร้างโมเดล ข้อไหนแค่จดไว้ |
| **เชื่อมวิชา ML** | brief นี้คือ input ของขั้น modeling ทีมเอาโครงเดียวกันไปเขียนให้ข้อมูลโปรเจกต์ตัวเอง |

---

## ภาพรวมสายเดิม + สาย EDA

| สัปดาห์ | สายเดิม (`tutorial_flights` + ตัวตรวจ) | สาย EDA (`flights`) | ผลงานที่ส่งต่อไปสัปดาห์ถัดไป |
|---|---|---|---|
| 5 | จัดหน้า · Markdown · Native Filter · กราฟ ≥ 4 | ค่าหาย | แท็บสำรวจข้อมูล + Decision log เริ่มต้น |
| 6 | แท็บ ≥ 2 · cross-filter | การกระจาย | รู้ว่าต้องใช้ median และตัดแกนที่ 180 |
| 7 | rolling · time comparison · resample · annotation | outlier | เกณฑ์ flag `> 180` |
| 8 | virtual dataset · SQL GROUP BY · metric กลาง | ยืนยันด้วยตัวเลข | `flights_clean` + `on_time_rate` |
| 9 | แผนที่จุดดิบ · แผนที่รวมจุด | ความสัมพันธ์ | รายชื่อตัวแปรที่ใช้ได้/ห้ามใช้ |
| 10 | export · ชื่อกราฟ · cache | ส่งต่อ | Data Understanding Brief |

## สิ่งที่ต้องทำก่อนใช้จริง

1. **แก้เอกสารบทที่ 5** ตารางชุดข้อมูลบอกว่ามี `flight_data` และ `airports` ซึ่งไม่มีอยู่จริง ของจริงคือ `flights` (รวมพิกัดแล้ว) — นักศึกษาที่อ่านจะไปหาชุดที่ไม่มี
2. **ทดสอบ Bubble Chart entity = `rowid` บนเซิร์ฟเวอร์จริง** ก่อนสัปดาห์ 9 (ยืนยันจากเวอร์ชันแล้ว ยังไม่ได้สร้างกราฟจริง) ถ้าหนักเกินไปให้กรองสายการบินเดียวหรือสุ่มด้วย `rowid % 10 = 0`
3. **สัปดาห์ 7 สายเดิมต้องการ rolling mean 7 วัน** แต่ `flights` มี 4 วัน — สาย EDA จึงใช้ Box Plot กับชั่วโมงของวันแทนการวิเคราะห์รายวัน ไม่กระทบตัวตรวจเพราะข้อกำหนดตรวจที่ `tutorial_flights`
4. **ควิซ QW05–QW10** ต้องเขียนใหม่ให้ถามการอ่านกราฟของขั้นนั้น ๆ (ของเดิมยังมีเนื้อหายุค Power BI ปนอยู่)
5. **เวลาในคาบ** ตอนที่ 1–4 ของแล็บ 5 ย่อจาก 100 นาทีเหลือ 60 ถ้าจริงแล้วไม่พอ ให้ตัด 5.4 ไปสัปดาห์ 6

---

## สัปดาห์ 11 — บทสรุป: Superset ในงานจริง และให้ AI ทำงานบน Superset ผ่าน MCP

> สัปดาห์ 11 = ครั้งที่ 12 ของปฏิทินมหาวิทยาลัย (2–6 พ.ย. 2569) · ในสเปกเดิมสัปดาห์นี้เขียนว่า "Proposal นำเสนอหัวข้อ"
> ซึ่งไม่ตรงกับ timeline โปรเจกต์ร่วมแล้ว (Proposal ผ่านไปตั้งแต่ 14–19 ก.ย.) จึงว่างพอจะเป็นสัปดาห์สรุป

### สิ่งที่ตรวจแล้วเกี่ยวกับ MCP server ที่ทำไว้ (19 ก.ย. 2569)

| เรื่อง | ค่าจริงบนเซิร์ฟเวอร์ |
|---|---|
| ตัวที่รันอยู่ | คอนเทนเนอร์ `superset-mcp` image `superset-console:0.4.0` (โค้ดที่ `~/mcp-superset`) ต่อกับ Superset 4.1.1 ผ่าน service account `mcp_service` (role = Gamma + sql_lab + datasource access) |
| ทางเข้า | `https://mcp-superset.minddatatech.com/mcp` — proxy เปิดเฉพาะ `/mcp` (root ตอบ 404 ถูกต้องแล้ว) · เป็น OAuth 2.1 server ในตัว เพิ่มเป็น custom connector ใน Claude ได้ ผู้ใช้ล็อกอินด้วยบัญชี Superset ของตัวเอง แล้ว token ผูกกับสิทธิ์ของคนนั้น |
| tool ที่มี | อ่าน: `list_datasets` `get_dataset_info` `list_dashboards` `get_dashboard_info` `list_charts` `get_chart_info` `get_chart_data` `execute_sql` · เขียน (scope `write`): `generate_chart` (table / pie / big_number / timeseries / dist_bar เท่านั้น — **ไม่มี histogram, box plot, scatter**) `update_chart` `add_chart_to_existing_dashboard` `create_virtual_dataset` `save_sql_query` · ลบอยู่หลัง scope `delete` แยกต่างหาก |
| การควบคุม | `CONSOLE_REQUIRE_APPROVAL=true` (ต้องอนุมัติก่อนใช้) · audit log ทุก tool call · Superset บังคับ ownership เดิม |
| เอกสารบทที่ 13 ใน docs | **ไม่ตรงกับของจริง** — บทนี้เขียนถึง MCP ทางการของ Superset 6.1 ที่รันในเครื่อง (`127.0.0.1:5008` ผ่าน `.mcp.json`) แต่ของวิชาคือ superset-console บนโดเมนสาธารณะผ่าน OAuth และ Superset ที่ใช้คือ 4.1.1 ต้องเขียนบทนี้ใหม่ก่อนสัปดาห์ 11 |

### คาบทฤษฎี (2 ชั่วโมง) — "Superset อยู่ตรงไหนของงานข้อมูลจริง"

| ช่วง | เนื้อหา | เชื่อมกับที่เรียนมา |
|---|---|---|
| 1. ย้อนดู 10 สัปดาห์เป็นระบบเดียว (30 นาที) | ข้อมูลดิบ → dataset/semantic layer (สัปดาห์ 8) → กราฟ (2–4) → แดชบอร์ด (5–6) → วิเคราะห์เพิ่ม (7, 9) → ส่งมอบ (10) — วาดเป็นผังเดียวว่าแต่ละสัปดาห์คือชั้นไหนของระบบ | นักศึกษาเห็นว่าที่ทำมาไม่ใช่ 10 แล็บ แต่คือระบบ BI หนึ่งชุด |
| 2. Superset ในองค์กร (30 นาที) | ใครใช้ (analyst / ผู้บริหาร / ทีม data) · เทียบกับ notebook และเครื่องมือ BI เชิงพาณิชย์ · สิ่งที่ต้องดูแล: สิทธิ์ (role, RLS) · แคช · Alerts & Reports (เปิดอยู่บนเซิร์ฟเวอร์วิชา) · **เมื่อไรไม่ควรใช้ Superset** (งาน wrangling, สถิติหนัก, รายงานพิมพ์) | บทที่ 2, 4, 10 |
| 3. AI ทำงานบน Superset ผ่าน MCP (45 นาที) | MCP คืออะไร · tool ที่ AI เรียกได้จริงบนเซิร์ฟเวอร์วิชา (ตารางข้างบน) · AI ทำได้เท่าสิทธิ์ของบัญชีที่ล็อกอิน ไม่มากกว่านั้น · audit log: ทุกคำสั่งมีคนรับผิดชอบ · สาธิตสด: สั่ง AI สำรวจ `flights_clean` แล้วเทียบกับที่นักศึกษาหาเองในสัปดาห์ 5–8 — **ตรงไหนที่ AI ได้คำตอบต่างจากคน และทำไม** | บทที่ 13 (ฉบับแก้ใหม่) |
| 4. กติกาการใช้ AI ในโครงงาน (15 นาที) | 5 ข้อจาก 13.4 + ข้อกำหนดว่าต้องเขียนกำกับว่าใช้ AI ตรงไหนและแก้อะไรต่อ | ใช้จริงในสัปดาห์ 12–15 |

### MCP เป็นเดโมโชว์เคส ไม่ใช่แล็บ (ตัดสินใจ 19 ก.ย. 2569)

นักศึกษา**ไม่ต้อง**ต่อ MCP เอง ไม่มีข้อกำหนดตัวตรวจใหม่ ไม่ต้องเปิดการอนุมัติหมู่
อาจารย์เดโมสดในคาบทฤษฎีจากบัญชีของตัวเอง ราว 30–40 นาที ตามสคริปต์นี้

| จังหวะ | สั่ง AI ว่า | สิ่งที่อยากให้นักศึกษาเห็น |
|---|---|---|
| 1. AI เป็นใคร | "ฉันเป็นใคร มีสิทธิ์อะไรบ้าง" (`get_instance_info`) | AI ทำได้เท่าบัญชีที่ล็อกอิน ไม่ใช่ผู้วิเศษ |
| 2. AI สำรวจซ้ำสิ่งที่ห้องเคยหาเอง | "นับค่าหายใน `flights` ทุกคอลัมน์ delay" แล้วเทียบกับตัวเลขสัปดาห์ 5 บนจอ (1,195 / 1,354) | AI หาได้เร็วกว่า แต่ได้เท่ากัน — ความเข้าใจว่าทำไมหายยังต้องเป็นของคน |
| 3. AI สร้างกราฟ | "สร้างกราฟ on_time_rate ตามชั่วโมงจาก `flights_clean` แล้ววางบนแดชบอร์ดเดโม" (`generate_chart` → `add_chart_to_existing_dashboard`) | จากคำสั่งภาษาไทยเป็นกราฟบนแดชบอร์ดในหนึ่งนาที แล้ว**ตรวจตัวเลขหนึ่งจุดด้วยมือ**ให้ดู (ชั่วโมง 21 ≈ 51%) |
| 4. จับผิด AI ต่อหน้าห้อง | สั่งกำกวมโดยตั้งใจ "แสดง delay เฉลี่ยของแต่ละสายการบิน" | ดูว่ามันใช้ mean หรือ median กรองเที่ยวบินยกเลิกหรือไม่ — ถ้าไม่กรอง ตัวเลขจะผิดแบบที่ห้องรู้แล้วจากสัปดาห์ 5 นี่คือจุดที่ทั้ง 6 สัปดาห์ของ EDA กลายเป็นเครื่องมือตรวจ AI |
| 5. ปิด | ให้ AI ไล่ว่าแดชบอร์ดเดโมมีกราฟไหนที่ไม่มีคำถามรองรับ (`get_dashboard_info`) | ใช้ AI ตรวจงานตัวเองได้ และกติกาใช้ AI ในโครงงาน (บทที่ 13.4) |

**สิ่งที่นักศึกษาส่งสัปดาห์ 11** เหมือนสัปดาห์อื่น: บันทึกสัปดาห์ที่ 11 ในกล่อง Markdown (ข้อกำหนดเดิมของตัวตรวจ)
หัวข้อที่ให้เขียน: จากเดโม AI ทำอะไรได้ดี ทำอะไรพลาด และคุณจะตั้งกฎอะไรให้ตัวเองก่อนใช้มันในโครงงาน
ทีมที่อยากใช้ MCP ในโครงงานจริง ให้มาขอเปิดสิทธิ์เป็นรายทีม (อนุมัติทีละทีมทำได้ ไม่ต้องอนุมัติ 138 คน)

### สิ่งที่ต้องทำก่อนสัปดาห์ 11

1. **ซ้อมเดโมด้วยบัญชีอาจารย์** ต่อ connector → 5 จังหวะข้างบน จับเวลาให้อยู่ใน 40 นาที และเตรียมแดชบอร์ดเดโมที่ `mcp_service` หรือบัญชีอาจารย์เป็น owner (ไม่งั้น `add_chart_to_existing_dashboard` จะติด ownership)
2. **`generate_chart` ทำได้ 5 ชนิด** (table / pie / big_number / timeseries / dist_bar) — สคริปต์เดโมใช้ timeseries/dist_bar อยู่แล้ว อย่าสั่ง histogram หรือ box plot บนเวที
3. **บทที่ 13** แก้เฉพาะส่วนที่ผิดจากของจริง (URL, OAuth, รายการ tool) ไม่ต้องรื้อทั้งบท เพราะนักศึกษาไม่ต้องทำตามเอง
4. **ความปลอดภัย** API key ของ MCP (scope write) อยู่ใน environment ของคอนเทนเนอร์ อ่านได้ด้วย `docker inspect` — ควรย้ายไป secrets file และหมุนคีย์ แม้ไม่เปิดให้นักศึกษาใช้ก็ตาม
